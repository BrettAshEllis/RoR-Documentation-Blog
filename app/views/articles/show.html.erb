<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  # The edit_article_path helper method returns the path to the edit article page.
  <li><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
                  # turbo_method: :delete tells Turbo to send a DELETE request to the server when the link is clicked.
                  # turbo_confirm: "Are you sure?" tells Turbo to display a confirmation dialog before sending the DELETE request to the server.

                  # As per Copilot - "Turbo is a feature of Ruby on Rails that allows for fast and efficient web application updates without requiring a full page reload.
                  # It uses JavaScript to update specific parts of a web page, improving the user experience by reducing latency and providing a smoother browsing experience. 
                  # Turbo can be used with Rails' built-in link helpers, such as link_to, to send AJAX requests and update content dynamically."
</ul>

<h2>Comments</h2>
<% @article.comments.each do |comment| %>
  <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
  </p>

  <p>
    <strong>Comment:</strong>
    <%= comment.body %>
  </p>
<% end %>

<h2>Add a comment:</h2>
<%= form_with model: [ @article, @article.comments.build ] do |form| %>
  <p>
    <%= form.label :commenter %><br>
    <%= form.text_field :commenter %>
  </p>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
